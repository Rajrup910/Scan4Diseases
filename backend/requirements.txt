# Backend (FastAPI) dependencies.
#
# torch + torchvision are required for inference but are NOT pinned here, because
# they must come from the CUDA index on the dev machine and may come from the CPU
# index in deployment (Hugging Face Spaces has no GPU):
#
#   dev  : pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   prod : pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# Server-rendered doctor web portal (FastAPI + Jinja2, same origin, no CDN).
Jinja2>=3.1
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
Pillow>=10.3
numpy>=1.26
opencv-python-headless>=4.10
httpx>=0.27

# --- database & auth ---
SQLAlchemy>=2.0
bcrypt>=4.1
PyJWT>=2.9
email-validator>=2.1
# Fernet symmetric encryption for patient-shared images at rest (IMAGE_ENCRYPTION_KEY).
cryptography>=42

# --- dev / test ---
pytest>=8.3
pytest-asyncio>=0.24
